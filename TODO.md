1. Add concept of conversations (Every group of messages seperate by 30 minutes without messages per chat/channel). Bake into db like we do with all messages
2. Add a "Show in context" button to message cards (Language and interactions tabs). Open in a new tab. HTML files are in data but it should jump to the spot where the message is
3. Add aggregate count for kicking by kicker, kickee and by chat
4. "You changed the group description" captured in signal chat name changes
5. Better display of chat and nickname changes
6. Aggregate soundcloud links like youtube
7. Add a way for the scheduler to restart the api to load the new db. Maybe a restart endpoint
8. Add graph for platform usage over time
