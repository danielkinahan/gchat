themes:
  - name: Main group chat
    # Optional: emoji shown in the Themes filter chip
    emoji: "💬"
    channels:
      - source: "Discord: Example Server"
        channel: general
      - source: "Signal: signal"
        channel: "Main Group"
      - source: "Facebook: Example Chat"
        channel: "Example Chat"

  - name: Random
    emoji: "🎲"
    channels:
      - source: "Discord: Example Server"
        channel: random
